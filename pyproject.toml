[project]
name = "job-scheduler"
version = "0.1.0"
description = "A pluggy-based plugin management system for alpha-miner workers"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Pham Tu", email = "tu@orai.io" }]
keywords = [
    "pluggy",
    "plugins",
    "scheduler",
    "fastapi",    
    "trading",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Framework :: FastAPI",
]

dependencies = [
    "pluggy>=1.0",
    "pydantic>=2.0",
    "APScheduler>=3.10",
    "sqlalchemy==2.0.45",
    "fastapi>=0.100",
    "uvicorn[standard]>=0.20",
    "alembic>=1.16.5",
    "psycopg[binary]>=3.1.0",
    "casbin>=1.43.0",
    "passlib>=1.7.4",
    "python-jose==3.5.0",
    "python-multipart>=0.0.21",
    "sqlglot",
    "casbin-redis-adapter>=1.5.0",
    "greenlet>=3.3.0",
    "pydantic-settings>=2.12.0",
    "jinja2>=3.1.6",
    "httpx>=0.28.1",
    "asyncpg>=0.31.0"    
]

[project.urls]
"Homepage" = "https://github.com/oraichain/job-scheduler"
"Repository" = "https://github.com/oraichain/job-scheduler.git"
"Issues" = "https://github.com/oraichain/job-scheduler/issues"

[project.optional-dependencies]
dev = ["pytest", "ruff", "black"]

[project.scripts]
alpha-miner-plugins-server = "server:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E", "W", "F", "I", "B", "UP"]
ignore = []

[tool.pytest.ini_options]
addopts = "-ra -q"

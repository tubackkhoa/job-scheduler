[request_definition]
r = sub, obj, act, ctx

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = (r.sub == p.sub || has_role(r.ctx, p.sub)) && r.obj == p.obj && r.act == p.act
